server:
  port: 8081

spring:
  application:
    name: iam-service
  datasource:
    url: jdbc:mysql://localhost:3306/edusphere_iam?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: pandavamadhumala@gmail.com
    password: ayyk gmvn idrf hral
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

app:
  jwt:
    secret: edusphere-jwt-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm
    expiration-ms: 3600000
    refresh-expiration-ms: 604800000
  service:
    secret: edusphere-internal-svc-2026
  frontend-url: http://localhost:3000

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info
